import { TemplateStringOption } from "../shared/template";

export type EntityDeleterTemplateValues = {
  entityName: string;
};

export type EntityDeleterTemplateOptions = {
  entityName: TemplateStringOption;
};
