import { TemplateStringOption } from "../shared/template";

export type EntityRepositoryInterfaceTemplateValues = {
  entityName: string;
};

export type EntityRepositoryInterfaceTemplateOptions = {
  entityName: TemplateStringOption;
};
