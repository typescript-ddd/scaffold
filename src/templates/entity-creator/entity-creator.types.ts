import { TemplateStringOption } from "../shared/template";

export type EntityCreatorTemplateValues = {
  entityName: string;
};

export type EntityCreatorTemplateOptions = {
  entityName: TemplateStringOption;
};
