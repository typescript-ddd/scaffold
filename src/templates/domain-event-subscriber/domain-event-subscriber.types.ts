import { TemplateStringOption } from "../shared/template";

export type DomainEventSubscriberTemplateValues = {
  eventName: string;
};

export type DomainEventSubscriberTemplateOptions = {
  eventName: TemplateStringOption;
};
